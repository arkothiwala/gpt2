model:
  type: gpt2
  n_layers: 12
  n_heads: 12
  d_model: 768
  # d_ff: 3072
  vocab_size: 50257
  dropout: 0.1
  activation: gelu
  position_embedding: learned
  context_length: 1024

tokenizer:
  type: gpt2
  vocab_size: 50257

optimizer:
  type: adam
  lr: 2.5e-4
  weight_decay: 0.01
  beta1: 0.9
  beta2: 0.999

scheduler:
  warmup_start_factor: 0.00001
  warmup_end_factor: 1.0
  warmup_steps: 2000
  schedule: cosine

# all configs for training loop
training:
  global_batch_size: 512
  micro_batch_size: 4
  epochs: 100
  valid_epoch_interval: 5
  test_epoch_interval: 5
  init_std: 0.02
  dataloader_num_workers: 8
  checkpoint_interval: 10

# all configs for dataset and dataloader
data:
  train_bin_file_path: assets/processed_data/openwebtext_train.bin
  valid_bin_file_path: assets/processed_data/openwebtext_valid.bin
  test_bin_file_path: assets/processed_data/openwebtext_test.bin
  prefetch_factor: 2